# Absolution path of the directory containing dyninstAPI, patchAPI ...
DYNINST_DIR=

#`pwd`, self-propelled instrumentation root directory's absolute path
SP_DIR=

# Set 'true' to build agent as a small shared library that relies on other
# shared libraries, e.g., dyninst libraries; 
# otherwise, a single huge shared library that static-linked all libraries.
DYNLINK=

# Set it to x86_64-unknown-linux2.4 for x86-64, or i386-unknown-linux2.4 for x86
PLATFORM=
